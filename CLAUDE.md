# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

This is a personal GitHub profile repository (`Sh1n1230/Sh1n1230`). The `README.md` is displayed on the GitHub profile page. The only asset is `assets/Loader cat.svg`, an animated SVG used as the profile visual.

## Structure

- `README.md` — GitHub profile page content (rendered on github.com/Sh1n1230)
- `assets/Loader cat.svg` — Animated SVG loader/cat illustration

## No build or test pipeline

There are no dependencies, build steps, or tests. Changes are effective once pushed to `main`.
